export interface IDivision {
    name: string,
    slug: string;
    thumbnail?: string,
    description?: string,
};


/**
 * division name = Chattrogram Division
 * 
 * slug = chattrogram-divison
 * 
 * /:id => /31354745
 * 
 * /:slug => /divison/chattrogram/division
 * 
 * slug diye single division khuje naoya jabe
 */